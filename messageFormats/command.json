{
  "header" : {
      "type" : "REQUEST || UPDATE || ERROR || STOP || ACCEPTED",
      "msg" : "Use for errors and other cases",
      "sequence" : 1
  },

  "commands" : [{
    "function" : "NAME",
    "parameters" : []
  }]
}
